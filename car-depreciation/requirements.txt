# Install with:  pip install -r requirements.txt
#
# Lower bounds only, so pip resolves versions that suit your Python. Tested on
# Python 3.10-3.13 with numpy 2.x, pandas 2.x.

numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7

# Only needed to regenerate the .xlsx sample via build_astra_sample.py.
openpyxl>=3.1
